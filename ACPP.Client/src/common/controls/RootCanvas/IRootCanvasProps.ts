export interface IRootCanvasProps {

    // Children to be passed to the Root Canvas component
    children: React.ReactNode;

    // Optional styling to be applied to the Root Canvas component
    className?: string;

};